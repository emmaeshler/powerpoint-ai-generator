/**
 * Claude Direct Integration — Legacy file
 * The MCP wrapper integration is now handled directly in App.tsx
 * via the handleRequestClaudeGeneration function.
 */
export const INTEGRATION_NOTE = 'MCP integration is now component-based. See claude-generator.ts for the system prompt.';
